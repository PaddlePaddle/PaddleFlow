{{- if $.Values.paddleflow_server.logExporter.enable }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    prometheus: kube-prometheus
    app: {{ template "paddleflow-server.paddleflow_server.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    app.kubernetes.io/managed-by: '{{ $.Release.Service }}'
    app.kubernetes.io/instance: '{{ $.Release.Name }}'
    helm.sh/chart: '{{ $.Chart.Name }}-{{ $.Chart.Version }}'
    app.kubernetes.io/name: '{{ template "paddleflow-server.paddleflow_server.fullname" $ }}'
  name: {{ template "paddleflow-server.paddleflow_server.fullname" . }}-log-exporter
spec:
  endpoints:
  - interval: 10s
    port: log-metric
    scrapeTimeout: 10s
  namespaceSelector:
    any: true
  selector:
    matchLabels:
      app: {{ template "paddleflow-server.paddleflow_server.fullname" . }}
{{- end }}

{{- if and .Values.paddleflow_server.service.ports (not .Values.paddleflow_server.service.extra_usr_define_services) }}
{{- if .Values.paddleflow_server.service.prometheus_scrape }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app: {{ template "paddleflow-server.paddleflow_server.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    heritage: "{{ .Release.Service }}"
    release: "{{ .Release.Name }}"
    app.kubernetes.io/managed-by: '{{ $.Release.Service }}'
    app.kubernetes.io/instance: '{{ $.Release.Name }}'
    helm.sh/chart: '{{ $.Chart.Name }}-{{ $.Chart.Version }}'
    app.kubernetes.io/name: '{{ template "paddleflow-server.paddleflow_server.fullname" $ }}'
  name: {{ template "paddleflow-server.paddleflow_server.fullname" . }}-svc
spec:
  endpoints:
  {{- range $port_name, $port_content := .Values.paddleflow_server.service.ports }}
  {{- if $port_content.prometheus_scrape }}
  - interval: {{ default "10s" $port_content.prometheus_scrape_interval }}
    {{- with $port_content.prometheus_scrape_path }}
    path: {{ . }}
    {{- end }}
    port: {{ $port_content.name }}
  {{- end }}
  {{- end }}
  namespaceSelector:
    any: true
  selector:
    matchLabels:
      app: {{ template "paddleflow-server.paddleflow_server.fullname" . }}
{{- end }}
{{- end }}

{{- range $service_name, $service := .Values.paddleflow_server.service.extra_usr_define_services }}
{{- if $service.prometheus_scrape }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app: {{ template "paddleflow-server.paddleflow_server.fullname" $ }}
    chart: "{{ $.Chart.Name }}-{{ $.Chart.Version }}"
    heritage: "{{ $.Release.Service }}"
    release: "{{ $.Release.Name }}"
    app.kubernetes.io/managed-by: '{{ $.Release.Service }}'
    app.kubernetes.io/instance: '{{ $.Release.Name }}'
    helm.sh/chart: '{{ $.Chart.Name }}-{{ $.Chart.Version }}'
    app.kubernetes.io/name: '{{ template "paddleflow-server.paddleflow_server.fullname" $ }}'
  name: {{ template "paddleflow-server.paddleflow_server.fullname" $ }}-{{$service.service_domain}}
spec:
  endpoints:
  {{- range $port_name, $port_content := $service.ports }}
  {{- if $port_content.prometheus_scrape }}
  - interval: {{ default "10s" $port_content.prometheus_scrape_interval }}
    {{- with $port_content.prometheus_scrape_path }}
    path: {{ . }}
    {{- end }}
    port: {{ $port_content.name }}
  {{- end }}
  {{- end }}
  namespaceSelector:
    any: true
  selector:
    matchLabels:
      app: {{ template "paddleflow-server.paddleflow_server.fullname" $ }}
{{- end }}
{{- end }}
